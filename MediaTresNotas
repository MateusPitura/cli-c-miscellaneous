//Calculo da media de 3 notas

#include <stdio.h> 

int main(){
    printf("Informe as notas: ");
    int valor1, valor2, valor3, media;
    scanf("%d %d %d", &valor1, &valor2, &valor3);
    media = (valor1 + valor2 + valor3)/3;
    printf("A media de suas notas e %d", media);
    return 0;
}
